<h1>Products#index</h1>
<p>Find me in app/views/product/index.html.erb</p>
<p>
    <%= link_to "All", root_path %>
    <% @categories.each do |category| %>
        <span><%= link_to category.name, root_path(category_id: category.id) %></span>
    <% end %>
</p>
<% @products.each do |product| %>
    <p><strong><%= "#{product.name} -" %></strong> <%= "#{product.description} Prix: #{product.price}€ - #{pluralize(product.comments.visible.count, 'comment', plural: 'comments')} | " %><span><%= link_to 'Show', product %></span></p>
<% end %>

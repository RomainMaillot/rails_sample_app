<h1>Products#index</h1>
<p>Find me in app/views/product/index.html.erb</p>
<% @products.each do |product| %>
    <p><strong><%= "#{product.name} -" %></strong> <%= "#{product.description} Prix: #{product.price}€" %></p>
<% end %>
<%= will_paginate @products %>
